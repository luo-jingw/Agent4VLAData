# ISSUE-001

Status: open

Area: 数据处理管线

## Observation

现有几何重采样管线无法识别和去除遥操数据的次优解；本次调研（35 篇文献）确认
该问题在文献中无直接先例（GAP-03），且"语义过滤能否带来下游收益"无任何定量
证据（GAP-02）。agent 方案因此缺少可直接借鉴的方法与验收基线。

## Impact

- 不能直接开工实现 agent 管线：需要先补证据（受控对比实验）。
- 任何"语义打分→过滤"方案都面临 [paper_005](https://arxiv.org/abs/2606.10229) 的脱钩风险。

## Evidence

- `synthesis/evidence_matrix.md`（负证据 1、4；RQ1.3/RQ1.4）；
- `synthesis/gap_list.md` GAP-02、GAP-03、GAP-04；
- 现有方法包：`reference/g2-orion05-resample-training/docs/`。

## Hypotheses

1. 语义信号与几何信号正交，可以叠加（待验证）。
2. 次优段的语义特征（犹豫、绕路）可由 VLM 进度曲线涨落检出（[paper_010](https://arxiv.org/abs/2509.17321) 定性
   案例支持，未定量验证）。
3. 加权（而非删除）更安全：不丢数据，仅调整监督密度（无证据）。
4. （2026-09-06）key_weight=3.0 落在"源帧在事件窗"上，而事件窗是状态歧义（state aliasing）
   重灾区（腕相机过近、观测不可分）——×3 可能在强调不可学习的噪声；关键
   动作信号已在接近段帧的 chunk 里。待测：事件窗内 vs 窗外源帧的视觉可分性
   分布（见 opportunities.md OPT-001 旋钮 4 证据 6）。

## Next Experiment

按 `opportunities.md` OPT-001 的证据顺序执行：

1. （无训练、最便宜）**浪费点占比基线**：现有曲率管线 + 人工标注 20–50 个
   episode 的次优段 → 测"分配在次优段上的点数 / H"的基线分布。
2. （无训练、最便宜）**事件窗可分性分布**：视觉邻居一致性（CLIP 嵌入 kNN，
   视觉相似帧动作发散 ⇒ 不可分）算全集的逐帧可分性 → 比较事件窗内 vs 窗外
   源帧的分布，检验 key_weight=3.0 是否落在状态歧义重灾区（假设 4）。
3. （无训练）**事件泛化检出**：挑 5–10 个无 EE 切换的关键事件样本，看
   VLM/agent 能否标出人工认可的事件区间（人工抽查口径）。
4. （有训练）三臂受控对比：纯几何 / 几何+语义过滤 / 几何+语义加权 →
   同策略训练 → 成功率对比 + 成本。
5. 若坚持专利方向：专利库 prior art 检索（列在 4 之前）。

## Resolution

（待填）

# ISSUE-002

Status: open

Area: 数据处理管线（observation 表征）

## Observation

（2026-09-06）遥操与模型输入之间存在 observation 表征 gap。
操作员做动作所依据的信息（肉眼全视场、原始分辨率、力反馈、对延迟观测的
反应）部分不在模型观测（处理后相机 + 状态）中。后果：某些段的动作标签
在给定模型观测下**不可学**（隐变量条件）。与 state aliasing 同族但成因不同：
aliasing 是"观测相同动作不同"，gap 是"动作所依赖的信息根本不在观测里"。

## Impact

- 精细对齐/插入类段落的标签可能不可学；几何管线（看曲率）与语义管线
  （看任务）都识别不出这类段。
- key_weight 若落在这类段上会放大噪声（与 ISSUE-001 假设 4 同向）。

## Evidence

- LfD 的 correspondence problem（[paper_012](https://arxiv.org/abs/1907.03146) §6.4 提及，无解决方案文献）。
- 23+ 篇文献无"遥操信息通道 vs 模型观测通道"的 gap 诊断先例。

## Hypotheses

1. 探针不可学性（处理后观测 → 动作的预测失败段）可作为 gap 段的可测代理
   ——即旋钮 4 可分性标注的泛化。
2. 采集侧注意力标注（操作员当时看显示器还是直接视野）可降低诊断难度。
3. 反应时延可用观测-动作互相关估计；标签平移只敢在自由段做。

## Next Experiment

先做假设 1 的离线测量（无需训练模型以外的探针训练）；与 ISSUE-001 的
事件窗可分性测量合并执行。

## Resolution

（待填）

# ISSUE-003

Status: open

Area: 数据处理管线（终态歧义，2026-09-07 协作发现）

## Observation

放件完成（回到 home、吸盘空载）后策略不停，继续下探（像要抓取）。
诊断（详见协作方报告 WHY_IT_DOES_NOT_STOP.md，协作侧保存）：
四任务共用同一 home 位姿（跨集最大关节差 0.0036 rad），home 在四个任务中
承担四种角色：放件起始（持件·走）/ 放件末尾（空·停）/ 取件起始（空·走）/
取件末尾（持件·停）。吸盘状态被对角线复用抵消（home 处走动比例
持件 71.5% vs 空载 77.3%，互信息约 0）；prompt 在数据上完全可分（条件
分布 3.2% vs 100%），但末尾 home 帧 prompt 行为信噪比仅 2.8x（中段
75.1x），视觉先验压倒。终止帧 0/60 停止（仅放件对照模型 60/60）。

## Impact

- 多任务部署出现任务间粘连：终态与另一任务的起始态在观测上重合。
- 与观测可分性标注直接相关：该帧是"同一观测、不同要求动作"的
  歧义实例，且**歧义不等于信息缺失**——prompt 信息完整（编码探针 100%），
  是"模型在该状态不使用 prompt"的学习问题，不是观测契约缺口
  （与 ISSUE-002 成因不同）。

## Evidence

- 测量方法与表格见协作报告；论文先例：
  [BayesVLA](https://arxiv.org/abs/2512.11218)（H(l|v) 信息论判据）、
  [Grant 2026](https://arxiv.org/abs/2603.19233)（语言敏感度随任务歧义变化）、
  [Dong 2026](https://arxiv.org/abs/2606.11906)（逐步语言敏感度）、
  [模态坍缩](https://arxiv.org/abs/2601.04052)（视觉先验压倒稀疏语言）、
  [SALT](https://arxiv.org/abs/2608.10484)（子任务辅助 CE）、
  [M3](https://arxiv.org/abs/2608.22419)（结构化视觉遮蔽）、
  [Amnesic Probing](https://arxiv.org/abs/2006.00995)（编码不等于使用）。

## Hypotheses

1. 根因是任务定义：place 终止态设计为"回 home"，把终态送入 pick 起始态；
   若"放完即止、回位归调度器"，链路第一环即断。
2. 歧义帧重加权（w 3/10）通过 action_loss_weight 列（与现有 key_weight
   同一通路）可把损失份额拉回来——机制已有，剂量待网格。
3. 线性探针/编码性只能作下界诊断，不能证明"模型会使用"——
   Amnesic Probing 的 amnesic counterfactual 是该心智陷阱的标准判据。

## Next Experiment

- 检测先行：几何代理（末端离 home < 60 mm）歧义掩码 + H(l|v) 测出逐帧
  语言必要度（训练前门控的数据卡片）。
- 训练网格 G1/G2/G3 按协作报告执行（重加权/辅助 CE），护栏 6 项 + 主判据
  停止率 >= 48/60。
- 终态设计决策：与采集/部署侧确认"place 结束位置"语义（释放点 vs home）。

## Resolution

（待填）
