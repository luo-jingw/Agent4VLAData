# OPT-001

Status: proposed

Area: 数据处理管线（几何重采样 → 语义级处理）

## Observation

现有管线（`reference/g2-orion05-resample-training/`）用纯几何曲率重采样构造
动作标签：停顿自动失去监督密度，但遥操的次优解（绕路、犹豫、可完成但非最优）
在几何上与最优轨迹不可区分，无法去除。
调研（2026-09，23 篇文献）发现：文献中的演示过滤全部针对缺陷/失败演示，
"成功但次优"演示的识别与过滤是空白（`synthesis/gap_list.md` GAP-03）；
同时离线评分指标与下游策略收益脱钩的负面证据存在（[paper_005](https://arxiv.org/abs/2606.10229), [paper_014](https://arxiv.org/abs/2606.15064)）。

## Opportunity

用 agent（VLM/LLM + 分析工具）做语义级数据处理。经与用户讨论（2026-09-06）
收敛为**三个具体旋钮**（agent 可介入的最小语义决策点）：

- **旋钮 1：事件窗口泛化（语义事件检测）**。现有"事件"= 末端执行器命令沿 +
  真空反馈，只是"准静态假设破裂处"的特例代理：稳态场景中唯一需要时间保护
  的是 EE 状态切换；而插入 U 盘类任务的关键事件是空间接触事件（插到位），
  无 EE 切换，现有规则失明。agent 的任务：检测"驻留关键事件"，EE 切换只是
  特例。收益：训练侧事件窗定义泛化（零样本、任务级、可解释，区别于
  [paper_011](https://arxiv.org/abs/2603.01465) 的逐任务训练检测器）；推理侧 dt_min 时间保护同步泛化到插入类
  事件（VLA 部署"一冲而过"失败模式的解药）。
- **旋钮 2：稀疏点预算的二次筛选（次优段饿死）**。验证：`ds ∝ |q''|^(1/3)`，
  绕路=拐弯=高曲率 → 曲率度量**主动把点分给绕路**，次优解从最优段偷走点预算。
  可测指标：每 episode"浪费点占比" = 分配在 agent 标记次优段上的点数 / H。
  这是 agent 方案价值的第一观测指标（介入前 X% → 介入后 Y%，下游成功率不降）。
- **旋钮 3：事件预算自适应**。event_points=32、approach_frames=15 现为全局
  常量；agent 按任务/事件类型分配预算（插入类更密、抓取类沿用现状）。
  实现代价最小（只动参数不动结构），适合作 agent 的第一个落地旋钮。
- **旋钮 4：观测可分性标注（状态别名标注）**。用户提出：关键窗口（吸盘贴近
  工件）腕相机距离过近，observation 看不到有效信息，前几帧反而能给出更优动作。
  文献名：state aliasing（[paper_011](https://arxiv.org/abs/2603.01465) 的动机）。标注"该帧观测是否承载预测
  所需信息"（每帧可分性标签 + provenance 记录判据）。**标注必须对模型实际
  输入的表示（缩放/裁剪后的帧）判定，不是原图**——原图可见不代表模型分辨率
  下可见；因此该标注在 Observation 处理之后计算。两种独立测量可交叉验证：
  (1) 确定性视觉邻居一致性（CLIP 嵌入 kNN，视觉相似帧动作发散 ⇒ 不可分，
  纯离线无训练）；(2) VLM 可见性判断（"工件与吸盘相对位置可见吗"，具体感知
  问题，可用 SAM/Grounding DINO 兜底，是 VLM 最可靠的能力区）。
  **尖锐推论待测**：现有 key_weight=3.0 落在"源帧在事件窗"上，而事件窗恰是
  别名重灾区——×3 可能在强调不可学习的噪声。关键动作的信号已在接近段帧的
  chunk 里（chunk 重叠）。首个测量：事件窗内 vs 窗外源帧的视觉可分性分布。
  后续用法选项：key_weight 落点迁移 / 不可分帧降权 / 部署侧历史注入
  （[paper_011](https://arxiv.org/abs/2603.01465) 路线）或控制侧"最后一个信息充分帧决策"。
  **升级（用户补充 2026-09-06）**：关键帧判定本身 = 语义事件 ∧ 观测可分——
  EEF 状态变化发生时若观测被遮挡，该帧不设关键帧，权重迁到最近可分帧
  （其 chunk 仍覆盖事件时序）。

整体机会形态（过滤/加权/标注定位）沿用此前方案 A/B/C：
- 方案 A：语义打分 → 过滤次优段（对应 GAP-03，原创性最高）；
- 方案 B：语义打分 → loss 加权而非删除（与现有事件窗加权同构，侵入最小）；
- 方案 C：agent 做解释性标注 → 人工抽查 → 再决定过滤（对应 AgiBot 人工核对
  环节，风险最低）。
依据：`docs/reports/discussion_report.md` §4。

## 专利性初判（2026-09-06，非法律意见）

- 几何核（曲率密度 + 经典指数 1/3）是教科书结果，单独申请 novelty 薄弱；
  用户判断"传统小模型 VA 已有"方向正确。
- 潜在可辩护核心 = 系统组合："agent 自主决定标签构造旋钮（语义事件检测 +
  预算分配）+ 几何执行 + 验证回路"。须避开 [paper_011](https://arxiv.org/abs/2603.01465)（训练好的语义关键帧
  检测器）与 GVL（进度预测策展）的权利要求边角。
- 前置条件：专利库 prior art 检索；组合权利要求需要可测的协同效应证据
  （= Required Evidence 的下游对比），故顺序是证据先行、专利后评。
- 现实风险：ML 数据管线专利在部分法域面临抽象客体（Alice 类）审查；
  且本包已在 2026-08 起随协作分发（自披露计时已开始）。

## Expected Mechanism

VLM 对逐帧任务进度/关键帧质量的语义判断（GVL/VOC 路线，[paper_010](https://arxiv.org/abs/2509.17321)）能区分
"必要停顿"（真空建立）与"犹豫/绕路"（次优行为），从而在保留事件窗保护的同时
去除次优解的一部分。语义信号与几何信号正交：几何管"表示误差"，语义管
"演示质量"。

## Required Evidence

1. 受控对比实验：同数据 × 三臂（纯几何 / 几何+语义过滤 / 几何+语义加权）
   → 同策略训练 → 成功率对比 + 成本记录（每 1000 帧模型调用次数与美元）。
2. 语义打分的检出能力：对人工标注的次优段做检出评估（不能用检出 AUROC 替代
   下游验证——[paper_005](https://arxiv.org/abs/2606.10229) 的教训）。
3. 开源 VLM 在该数据上的 VOC 类指标是否脱离随机水平（[paper_010](https://arxiv.org/abs/2509.17321) 显示小模型
   接近随机）。
4. **浪费点占比**：介入前 vs 介入后，每 episode 分配在 agent 标记次优段上的
   点数 / H 的分布变化（旋钮 2 的第一观测指标，无下游训练也可先测）。
5. 事件泛化检出：agent 在插入类（无 EE 切换）任务上能否标出人工认可的
   关键事件（旋钮 1 的最小验证，人工抽查口径）。
6. 事件窗内 vs 窗外源帧的视觉可分性分布（旋钮 4 的首个测量，纯离线无训练；
   若窗内帧系统性不可分，key_weight=3.0 的落点需迁移）。
7. 专利方向若被 PI 坚持：专利库 prior art 检索结果（列于证据 1 之前）。

## Promotion Condition

进入 plan.md 的条件：证据 4/5 的离线可测项达成（浪费点占比可测、事件检出经
人工抽查），且证据 1 的实验设计获批。证据 1 的下游结果达标后，再评估
证据 6（专利）。未满足前保持 proposed。

# OPT-002

Status: proposed

Area: 数据处理管线（几何捷径 + 可行性验证）

## Observation

用户提出新思路：利用数据集重建环境（目标物体跨 episode 随机、单 episode 内
局部一致），构建粗糙 real2sim 管线，做"删帧→跳步连接→碰撞检验→逐步稀疏"
的朴素优化。调研确认该思路是**可行性验证轴**，与几何轴（表示误差）、语义轴
（演示质量）正交：现有包的贪心放点实验（RESAMPLING_REPORT §10）因缺
"评估假设轨迹的 oracle"被否决，real2sim 正是补这个 oracle；碰撞检验是最硬
的"执行反馈"形态（[paper_005](https://arxiv.org/abs/2606.10229), [paper_013](https://arxiv.org/abs/2503.03707) 的教训：执行反馈是唯一被证明有效的策展
依据）；最近先例是 [paper_021](https://arxiv.org/abs/2509.20070)（生成→模拟 rollout→成功才保存），但"从单
episode 视频重建场景"在调研文献中无先例。

**关键约束（用户补充，2026-09-06）：场景全部静态。** 成功判据形式化为：
路径几何一致（含搬运段工件刚性附着末端）∧ 吸盘切换停留时间一致；速度无关。
因此 oracle 退化为**纯静态碰撞检测**（场景几何 + 机器人连杆 + 附着工件 +
事件窗驻留），不需要动力学仿真；重建也只需静止几何，误差用障碍物保守膨胀
处理（误拒好捷径安全、误收碰撞路径致命）。

## Opportunity

两条可能的收益，按实现难度递进：
1. **标签稀疏化验证**：给现有曲率重采样加可行性约束，在碰撞检验监督下
   把标签压得更稀（现有方法不敢做的事）。
2. **捷径修复**：对绕路段提出跳步捷径，碰撞通过则替换原标签——直接去除
   空间次优解（现有几何与语义方案都做不到"生成更好的路径"）。
3. 与语义 agent 合成："语义指出哪段次优 + 几何生成捷径 + sim 验可行性"。

## Expected Mechanism

单 episode 多相机视频 → 静态场景重建（料箱/传送带/障碍物 + 工件拾取位姿；
搬运段工件刚性附着末端）→ 保守膨胀碰撞 oracle → 贪心稀疏化/捷径搜索
（禁止穿越事件窗）→ 通过者替换标签。无动力学、无速度仿真。

## Required Evidence

1. **oracle 自检**：重建场景回放原始遥操轨迹，碰撞报告必须为零。不过此关
   管线不可信（第一实验，风险最低）。
2. 稀疏化/捷径在重建场景中的通过率，及"通过捷径"与原轨迹的差异分布
   （能抓到多少绕路）。
3. 下游对比：新标签训练的策略成功率 vs 现有曲率管线（[paper_005](https://arxiv.org/abs/2606.10229) 教训，
   最终判据只能是下游）。
4. 重建成本（每 episode 的算力/时间）与漏检率（膨胀余量带来的误拒率）。

## Promotion Condition

进入 plan.md 的条件：证据 1 达成（oracle 回放零碰撞），且证据 2 显示捷径
显著非空。证据 3 达标后可作为替代/补充方案与 OPT-001 竞争。

## 与 OPT-001 的关系

- 两轴正交：语义判断"该不该改"，几何生成"怎么改"，sim 验证"改了能不能行"。
- OPT-002 不依赖大模型能力（小 VLM 接近随机的风险不存在），但依赖重建
  保真度；OPT-001 依赖模型能力，不依赖重建。可并行推进第一实验。
